func qasd() returns bool {

    asd as bool;
    asd = false & true;

    if(asd) {
        printk(1);

        if(true) {
            printk(2);
        } else {
            printk(3);
        }

        printk(4);

    }
    printk(7);





}
